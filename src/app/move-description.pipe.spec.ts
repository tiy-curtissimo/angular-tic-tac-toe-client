import { MoveDescriptionPipe } from './move-description.pipe';

describe('MoveDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new MoveDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
